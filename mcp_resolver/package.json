{
  "name": "agent-vm-mcp-resolver",
  "version": "0.1.0",
  "private": true,
  "description": "In-container MCP resolver for Agent VM",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "MCP_EXEC_AUDIT_ENABLED=false node --test ../tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.0.0"
  }
}
